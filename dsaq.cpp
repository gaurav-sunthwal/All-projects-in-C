#include <iostream>
using namespace std;

int main(int argc, char const *argv[])
{
    cout << "Hello, world!" << endl;
    int digit[] = {0, 1, 2, 3, 4, 5, 6, 7};
    int last = sizeof(digit) / sizeof(int);
    cout << digit[last];
    for (int i = 0; i < end(digit) - begin(digit); i++)
    {
    }

    return 0;
}
